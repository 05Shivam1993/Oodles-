* Run the requirements.txt file to install the packages by using the command "pip install -r requirements.txt"
* Read the API documentation file "REQUEST_JSON_RESPONSE.docx" for processing request and response
